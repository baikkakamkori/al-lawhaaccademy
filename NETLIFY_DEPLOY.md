# Netlify deployment

This is a Vite + React + Firebase project.

Build command: `npm run build`
Publish directory: `dist`

The Tailwind CDN is included in `index.html`, so no Tailwind npm package is required for this version.

Firebase features require Anonymous Authentication and Firestore rules to be configured in the Firebase project.
